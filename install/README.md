# Install

```shell
# For Ubuntu:
git clone https://github.com/voldikss/oh-my-config
cd oh-my-config/install
source ./ubuntu_install.sh
ubuntu_install

# For Arch(not maintained now)
source ./arch.sh
arch_install
```
