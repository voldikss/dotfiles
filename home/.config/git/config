[user]
	# name = duanyunzhi
	# email = duanyunzhi@currentcad.com
	name = voldikss
	email = dyzplus@gmail.com
    signingkey = 4303109B34AF02524281C1ECA2C58EA89AF5ADB3
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    hist = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
    type = cat-file -t
    dump = cat-file -p
	lg = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(cyan)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    lg1 = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
    lg2 = log --graph --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(bold yellow)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' --all
[credential]
	helper = cache
[core]
    editor = nvim
	fileMode = false
	quotepath = false
    autocrlf = false
    safecrlf = true
	eol = lf
	pager = less -+X -+F
[color]
	ui = auto
[diff]
	tool = vimdiff
[difftool]
	prompt = false
[gpg]
	program = gpg
[github]
    user = voldikss
[pull]
	rebase = true
[fetch]
	prune = true
[push]
	default = current
[log]
	decorate = true
	abbrevCommit = true
[diff "color"]
	commit = blue
[init]
	defaultBranch = main
[branch]
	autosetupmerge = true
	autosetuprebase = always
[checkout]
	defaultRemote = origin
[commit]
	# gpgsign = true
[safe]
	directory = /home/alan/code/neovim
[ssh]
        postBuffer = 524288000
[http]
        postBuffer = 524288000
